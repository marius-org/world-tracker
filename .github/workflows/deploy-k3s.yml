name: World Tracker — k3s Deploy

on:
  push:
    branches:
      - k3s
    paths-ignore:
      - 'k3s/**'
      - '*.md'

env:
  IMAGE_NAME: mariuseu/world-tracker

jobs:

  build-and-push:
    name: Build & Push Docker Image
    runs-on: control-node

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag
        run: echo "IMAGE_TAG=${{ github.run_number }}" >> $GITHUB_ENV

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: ./app
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:k3s-latest
            ${{ env.IMAGE_NAME }}:k3s-${{ github.run_number }}

  scan:
    name: Security Scan (Docker Scout)
    runs-on: control-node
    needs: build-and-push

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Scan for CVEs
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ${{ env.IMAGE_NAME }}:k3s-${{ github.run_number }}
          only-severities: critical
          exit-code: true

  deploy:
    name: Deploy to k3s via ArgoCD
    runs-on: control-node
    needs: scan

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update image tag in manifest
        run: |
          sed -i "s|${{ env.IMAGE_NAME }}:.*|${{ env.IMAGE_NAME }}:k3s-${{ github.run_number }}|g" \
            k3s/deployment.yml

      - name: Commit and push updated manifest
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add k3s/deployment.yml
          git commit -m "Deploy world-tracker k3s-${{ github.run_number }} [skip ci]"
          git push
